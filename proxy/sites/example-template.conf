# ============================================================
# Example Site Configuration Template
# ============================================================
#
# HOW TO USE:
#   1. Copy this file:  cp example-template.conf myapp.conf
#   2. Uncomment all lines below (remove the leading "# ")
#   3. Replace every <PLACEHOLDER> with your actual values
#   4. Test the config:  docker exec nginx-proxy nginx -t
#   5. Reload the proxy: bash deploy.sh reload
#
# PLACEHOLDERS TO REPLACE:
#   <DOMAIN>           — Your domain name, e.g. app.example.com
#   <APP_NAME>         — A short name for your app, e.g. myapp
#   <CONTAINER>:<PORT> — Docker container name and port on proxy-net,
#                        e.g. myapp-web:8080
#
# PREREQUISITES:
#   - Your app's docker-compose.yml declares proxy-net as an external network
#   - The app's entry-point service is connected to proxy-net
#   - A Cloudflare Origin Certificate is placed at:
#       /etc/ssl/cloudflare/<DOMAIN>/cert.pem
#       /etc/ssl/cloudflare/<DOMAIN>/key.pem
#
# NOTE: We use Docker's embedded DNS resolver (127.0.0.11) with variables
# instead of static upstream blocks. This allows Nginx to start even if
# the backend app is not yet running — hostnames are resolved at request
# time, not at startup.
#
# ============================================================

# ---- HTTP → HTTPS redirect ----
# Redirects all plain HTTP requests to HTTPS for this domain.
#
# server {
#     listen 80;
#     listen [::]:80;
#     server_name <DOMAIN>;
#
#     location / {
#         return 301 https://$host$request_uri;
#     }
# }

# ---- HTTPS server block ----
#
# server {
#     listen 443 ssl;
#     listen [::]:443 ssl;
#     http2 on;
#     server_name <DOMAIN>;
#
#     # -- SSL certificates (Cloudflare Origin Certificate) --
#     ssl_certificate     /etc/ssl/cloudflare/<DOMAIN>/cert.pem;
#     ssl_certificate_key /etc/ssl/cloudflare/<DOMAIN>/key.pem;
#
#     # -- SSL configuration --
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
#     ssl_prefer_server_ciphers off;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 1d;
#     ssl_session_tickets off;
#
#     # -- Security headers --
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-XSS-Protection "1; mode=block" always;
#     add_header Referrer-Policy "strict-origin-when-cross-origin" always;
#     add_header Strict-Transport-Security "max-age=63072000; includeSubDomains" always;
#
#     # -- Dynamic DNS resolution --
#     # Docker's embedded DNS resolver allows Nginx to start even if
#     # the backend container is not running yet.
#     resolver 127.0.0.11 valid=30s;
#     set $backend <CONTAINER>:<PORT>;
#
#     # -- Location blocks --
#
#     # API routes (with rate limiting)
#     # location /api/ {
#     #     limit_req zone=api burst=20 nodelay;
#     #
#     #     proxy_pass http://$backend;
#     #     proxy_http_version 1.1;
#     #     proxy_set_header Host $host;
#     #     proxy_set_header X-Real-IP $remote_addr;
#     #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     #     proxy_set_header X-Forwarded-Proto $scheme;
#     # }
#
#     # Static assets (with long-term caching)
#     # location /static/ {
#     #     proxy_pass http://$backend;
#     #     proxy_http_version 1.1;
#     #     proxy_set_header Host $host;
#     #
#     #     add_header Cache-Control "public, max-age=31536000, immutable";
#     # }
#
#     # Default catch-all route
#     # location / {
#     #     limit_req zone=general burst=30 nodelay;
#     #
#     #     proxy_pass http://$backend;
#     #     proxy_http_version 1.1;
#     #     proxy_set_header Host $host;
#     #     proxy_set_header X-Real-IP $remote_addr;
#     #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     #     proxy_set_header X-Forwarded-Proto $scheme;
#     #     proxy_set_header Upgrade $http_upgrade;
#     #     proxy_set_header Connection "upgrade";
#     # }
# }
